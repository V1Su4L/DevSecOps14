pipeline{
    agent any
    environment{
        REPO_URL= 'https://github.com/devopsPRO27/Docker-exercise-3'
        IMAGE_NAME= 'resume'
        DIR_NAME= 'Docker-exercise-3'
        TAG= "V1.${BUILD_NUMBER}"
        CONTAINER_NAME= 'sorry'
        REGISTRY= 'hub.docker.com'
        ACCOUNT_NAME= 'liorlazar'
        NEW_IMAGE_NAME= 'reponame'
        DOCKERHUB_CREDENTIALS= credentials('Docker')
    }
    stages{
        stage("Clone"){
            steps{
                sh 'git clone ${REPO_URL}'
                sh 'pwd'
                sh 'find .'
            }
        }
         stage("Build"){
            steps{
                dir('Docker-exercise-3/ResumeMaker/'){
                    sh 'docker build -t ${IMAGE_NAME}:${TAG} --file ../../jenkins6/docker_agents/Dockerfile .'
                    sh 'docker images'
                }
            }
        }
        stage("Test"){
            when {branch 'Jenkins'}
            steps{
                echo "========Jenkins========"
            }
        }
        stage("Login to Dockerhub"){
            steps{
                sh 'echo ${DOCKERHUB_CREDENRTIALS}'
                sh 'echo ${DOCKERHUB_CREDENTIALS} | docker login -u liorlazar --password-stdin'
            }
        }
    }
    post{
        always{
            sh 'rm -fr ${DIR_NAME}'
            sh 'docker rmi -f ${IMAGE_NAME}:${TAG}'
        }
        success{
            echo "========pipeline executed successfully ========"
        }
        failure{
            echo "========pipeline execution failed========"
        }
    }
}